---
# === Project template ===
# Required: title, description
# Optional: tech, repo, url, date, draft
#
# `date` controls the sort order — newest first on /projects.
# `draft: true` hides the card from the page (handy for WIP).
title: "attention"
description: "Implementation of multi-head self-attention from scratch, plus benchmark scripts comparing it to PyTorch's built-in module."
tech: ["python", "pytorch", "cuda"]
repo: "#"             # link the card opens; falls back to `url`, then "#"
# url: "https://your-demo"
date: 2026-03-01
draft: false
---

Optional long-form notes about the project (renders only on a future detail
page — currently the index just shows the card).
